import React from 'react';

const AboutPage = () => {
    return (
        <div style={styles.container}>
            About the website.
        </div>
    );
};

const styles = {
    container: {
        flex: 1,
    }
}

export default AboutPage;
